{
  "name": "lurkerbot",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "6.6.0"
  },
  "scripts": {
    "start": "node ./node_modules/typescript/bin/tsc && node ./app.js",
    "test": "npm install && mocha test",
    "postinstall": "cd models && npm install && cd .. && node ./node_modules/typescript/bin/tsc"
  },
  "dependencies": {
    "discord.js": "^9.2.0",
    "lodash": "^4.15.0",
    "mkdirp": "*",
    "typescript": "^2.1.4"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.5.3"
  }
}
